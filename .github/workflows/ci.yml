name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Verdict compat gate (CON-001)
        run: |
          pip install "git+https://github.com/mrnicholasbcarter-code/verdict-core.git@main"
          verdict compat check --declared .verdict/compat-manifest.json --json

      - name: Lint with ruff
        run: ruff check .

      - name: Format check with ruff
        run: ruff format --check .

      - name: Type check with mypy
        run: mypy src/

      - name: Test with pytest
        run: pytest --cov=trade_risk_engine --cov-report=xml --cov-fail-under=80

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        if: matrix.python-version == '3.11'
        with:
          files: ./coverage.xml
          fail_ci_if_error: false

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install bandit safety

      - name: Run bandit security linting
        run: bandit -r src/

      - name: Run safety check
        run: safety check

  build:
    runs-on: ubuntu-latest
    needs: [test, security]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build twine

      - name: Build package
        run: python -m build

      - name: Check distribution
        run: twine check dist/*

      - name: Wheel install smoke
        run: |
          python -m venv "$RUNNER_TEMP/verdict-risk-wheel"
          "$RUNNER_TEMP/verdict-risk-wheel/bin/python" -m pip install --upgrade pip
          "$RUNNER_TEMP/verdict-risk-wheel/bin/python" -m pip install dist/*.whl
          "$RUNNER_TEMP/verdict-risk-wheel/bin/python" -c "from trade_risk_engine import RiskAuthority, RiskContext; assert RiskAuthority and RiskContext"
          "$RUNNER_TEMP/verdict-risk-wheel/bin/verdict-risk-benchmark" --help

      - name: Source distribution install smoke
        run: |
          python -m venv "$RUNNER_TEMP/verdict-risk-sdist"
          "$RUNNER_TEMP/verdict-risk-sdist/bin/python" -m pip install --upgrade pip
          "$RUNNER_TEMP/verdict-risk-sdist/bin/python" -m pip install dist/*.tar.gz
          "$RUNNER_TEMP/verdict-risk-sdist/bin/python" -c "from trade_risk_engine import RiskAuthority, RiskContext; assert RiskAuthority and RiskContext"
          "$RUNNER_TEMP/verdict-risk-sdist/bin/verdict-risk-benchmark" --help

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
