# Implementation Plan: README Accuracy and Feature Reconciliation

**Branch**: `001-readme-accuracy-and-feature-reconciliation` | **Date**: 2026-09-05 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `specs/001-readme-accuracy-and-feature-reconciliation/spec.md`

## Summary

Fix three broken `github.com/verdict/*` org links in README.md (unconditional); then
reconcile three features (Correlation Gate, Kelly Sizing, YAML desk controls) that are
documented in the README but absent from `src/trade_risk_engine/`. Real implementations of
all three exist in `~/kalshi-trader`. Owner decisions (Q1/Q2/Q3) govern which of port/
reference/roadmap disposition is taken. The unconditional link fix can be merged
independently; gated work awaits owner answers.

## Technical Context

**Language/Version**: Python 3.10+ (runtime 3.12.3; `pyproject.toml` targets `>=3.10`)

**Primary Dependencies**: msgspec>=0.18.0, pydantic>=2.0.0 (existing). pyyaml — added
only if Q3 = A. No new deps for link fix or Kelly port (stdlib math only).

**Storage**: N/A — pure in-process library

**Testing**: pytest + hypothesis (existing `dev` + `test` extras); ruff (lint); mypy (type)

**Target Platform**: Python library, cross-platform

**Project Type**: Library

**Performance Goals**: Existing gates target <1 µs hot path. New gate code MUST NOT
regress existing benchmark numbers (verifiable via `verdict-risk-benchmark` script).

**Constraints**: Files MUST NOT exceed 500 lines. New code MUST pass ruff and mypy
before PR.

**Scale/Scope**: Small — 1 README edit + up to 3 conditional source additions (one
module each for Kelly, cluster cap, YAML loader if chosen).

## Constitution Check

**Gate: Documentation Before Dependencies (Principle II)**
- Status: PASS. Research.md confirms implementations in kalshi-trader were read and
  cited before this plan was authored. No guesses about API shapes.

**Gate: Repository Boundaries (Principle III)**
- Status: PASS. All changes are scoped to `verdict-risk`; kalshi-trader code is read
  but not modified. Any port is a copy, not a cross-repo change.

**Gate: Verification Is Part of the Change (Principle IV)**
- Status: PENDING. Every gated feature MUST have a focused test and `pytest tests/`
  must pass before merge. Benchmark must not regress. Link fix requires HTTP 200
  verification (documented in quickstart.md).

**Gate: Safety and Least Authority (Principle V)**
- Status: PASS. README edit is reversible. Ported code is read-only math. YAML loader
  (if built) reads only a user-designated config path; no credentials or secrets.

## Project Structure

### Documentation (this feature)

```text
specs/001-readme-accuracy-and-feature-reconciliation/
├── plan.md              # This file
├── research.md          # Phase 0: evidence from kalshi-trader + verdict-risk
├── data-model.md        # Phase 1: RiskContext field additions (conditional)
├── quickstart.md        # Phase 1: validation guide
├── contracts/           # Phase 1: public API contracts for new symbols
│   ├── kelly.md         # Kelly sizing function contract (if Q2=A)
│   ├── correlation.md   # Cluster cap gate contract (if Q1=A)
│   └── yaml-loader.md   # YAML loader contract (if Q3=A)
└── tasks.md             # Phase 2 (generated by /speckit-tasks)
```

### Source Code (repository root)

```text
src/trade_risk_engine/
├── gates.py             # Existing gates; cluster cap added here (if Q1=A)
├── kelly.py             # New: Kelly sizing function (if Q2=A)
├── config.py            # New: YAML config loader (if Q3=A)
└── engine.py            # Existing; wired to new gates if Q1=A

tests/
├── test_kelly.py        # New (if Q2=A)
├── test_cluster_cap.py  # New (if Q1=A)
└── test_yaml_loader.py  # New (if Q3=A)
```

## Complexity Tracking

No constitution violations. No complexity exceptions required.
